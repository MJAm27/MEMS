import React from "react";
// ... (code อื่นๆ)

const ManagerDashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            {/* เนื้อหาของคุณ */}
        </div>
    );
};

// ตรวจสอบบรรทัดนี้: ต้องเป็น export default เท่านั้น
export default ManagerDashboard;